# windows的各服务操作
## zentao
- 超级管理：sudo -i
- 重启：/opt/zbox/zbox restart
- 已经加了自启：systemctl res
## nacos
- nacos启动：cd D:\nacos\bin；startup.cmd -m standalone
## python脚本
- 启动：nohup python3 /home/junwei/bt_http_simulator.py > simulator.log 2>&1 &
- 查看进程：ps -ef | grep bt_http_simulator
- 查看日志：tail -f simulator.log
- 停止：pkill -f bt_http_simulator.py
## 101claude
- export PATH="$HOME/.local/bin:$PATH"才能使用claude命令，然后直接导入配置
## kafka
- java启动(目前手动启动，后续用nssm来做注册进服务)
    - cd D:\kafka_2.13-4.2.0
    - .\bin\windows\kafka-run-class.bat kafka.Kafka config\server.properties
- 创建topic：
    ```
    .\bin\windows\kafka-topics.bat ^
    --bootstrap-server localhost:9092 ^
    --create ^
    --topic exchange-rate ^
    --partitions 1 ^
    --replication-factor 1
    ```
- 查看当前的topic：.\bin\windows\kafka-topics.bat --bootstrap-server localhost:9092 --list
- 发送消息(生产者)：.\bin\windows\kafka-console-producer.bat --topic test --bootstrap-server localhost:9092
- 接收消息(消费者)：
    ```
    .\bin\windows\kafka-console-consumer.bat ^
    --topic test ^
    --from-beginning ^
    --bootstrap-server localhost:9092
    ```
## redis
- 启动：redis-server
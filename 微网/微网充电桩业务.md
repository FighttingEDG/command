## 枪的状态不对，该桩充电中解决
- 查看是不是有正在充电状态的枪：select * FROM ec_charge_order  WHERE gun_id=000000000000451 and `order_status` =2;
- 手动修改该笔订单的充电状态：UPDATE ec_charge_order SET order_status = 1 WHERE gun_id = '000000000000451' AND order_status = 2;

## 当前有多少人在充电，充电的时候会冻结
-SELECT c.id, c.phone, c.total, c.frozen_amount
FROM ec_customer c
WHERE c.frozen_amount > 0
  AND NOT EXISTS (
    SELECT 1 FROM ec_charge_order o 
    WHERE o.create_user_id = c.id 
      AND o.order_status IN ('2', '6')
  )
ORDER BY c.frozen_amount DESC
LIMIT 500;

## 把“订单还在进行中（2/6）”和“枪当前状态（ec_pile）”对照起来，找不一致。
SELECT o.id, o.order_no, o.gun_id, o.order_status, o.update_time,
       p.lx_status, p.status AS pile_status, p.update_time AS pile_update_time
FROM ec_charge_order o
LEFT JOIN ec_pile p ON p.gun_id = o.gun_id
WHERE o.order_status IN ('2','6')
ORDER BY o.update_time ASC;

# 充电系统数据库表梳理

## 一、订单 / 支付 / 账户

### ec_charge_order（充电订单主表）
- 核心字段：
  - 订单信息：order_no / open_id / order_status / order_type
  - 设备关联：station_id / pile_id / gun_id
  - 费用：total / service_fee / elec_fee / discount_amount
  - 计量：soc / 电压 / 电流 / charge_quantity
  - 时间：开始时间 / 结束时间
  - 支付状态等

---

### ec_customer（用户账户表）
- 余额相关：total / frozen_amount
- 设备绑定：gun_id
- 用户信息：车牌 / 车型偏好
- 自动策略等

---

### ec_order_pay（通用支付流水）
- 字段：
  - order_id / total / transer_id / status
  - 扩展参数：parm1 ~ parm5

---

### ec_wx_order_pay（微信支付流水）
- 字段：
  - customer_id / total / transer_id / wxTranserId
  - status / type / manner / work_no / openId
- 关联：
  - ec_customer / ec_company / ec_user_group

---

### ec_charge_revenue（站点收入统计）
- 聚合统计：
  - SUM(revenue)
  - SUM(electric)
- 维度：ec_station

---

## 二、设备 / 枪口状态 / 充电过程

### ec_pile（充电桩表）
- 字段：
  - station_id / company_id / equipment_id
  - pile_name / equipment_type
  - work_status / gun_id / lx_status
  - power / report_status / qr_code

---

### ec_charger_port（充电枪参数）
- 参数：
  - voltage_upper / voltage_lower
  - electric_current / power
  - start_type / park / uniev_no
- 关联：
  - ec_wordbook（类型/标准/功率档）

---

### ec_gun_status_update_record（枪状态记录）
- 字段：
  - gun_id / before_status / after_status
  - gun_status / alert_code
  - period / charge_quantity
- 关联：
  - ec_pile_alert_manage / ec_pile / ec_company / ec_station

---

### ec_pile_start_to_charge_record（开始充电记录）
- 字段：
  - chargingStationID / equipmentID
  - result / msg / loginName / version
- 联动：
  - 更新 ec_charge_order（订单状态）
  - 更新 ec_customer（冻结金额/余额）

---

### ec_pile_reset_record（桩重置记录）
- 功能：
  - 记录桩重置
  - 可能联动：
    - 更新订单状态
    - 清空 ec_customer.gun_id

---

### ec_pile_upload_real_time_status_record（实时状态上报）
- 功能：
  - 更新 ec_pile
  - 更新订单部分字段

---

## 三、工单 / AI停车 / 摄像头

### ec_work_order（人工工单）
- 字段：
  - work_no / customer_id / car_number
  - appointment_*（预约信息）
  - power_start_time / power_end_time
  - power_quantity / power_length
  - status / cancel_reason / invoice

---

### ec_driver（司机/工作人员）
- 用于工单流程状态更新

---

### ai_work_order（AI工单）
- 字段：
  - number / work_type / station / parking_space
  - license_number / enter_time / send_time
  - state / processor
  - hand_start_time / check_time / stop_time
  - camera_event / parm1

---

### ai_work_order_remark（AI工单备注）
- 字段：
  - number / remark / remark_type

---

### ai_parameter_setting（AI参数配置）
- 配置项：
  - event_treated_time
  - stop_car_time_out
  - out_time_un_order_receiving
  - out_time_un_finish
  - expel_time

---

### ai_work_order_statistics（AI工单统计）
- 按日/站点/公司：
  - work_order_num
  - o_car_num / e_car_num
  - o_expel_success_num

---

### ai_data_statistics_num（识别统计）
- 字段：
  - car_num / o_car_num / e_car_num
  - parking_space
  - in_out_accuracy / recognition_accuracy
  - expel_success_num / expel_fail_num

---

### ec_camera_event（摄像头事件）
- 字段：
  - type / camera / parking_space
  - license_number / picture
  - state / employee
  - receipt_time / check_time / input_time

---

### ec_parking_space_rail（车位表）
- 摄像头通过 parking_space 关联

---

### ec_camera（摄像头表）
- 被 ec_camera_event 关联

---

### ec_parking_coupon_record（停车券记录）
- 字段：
  - request / response / url
  - car_no / send_time

---

## 四、活动 / 优惠券

### ec_activity（活动表）
- 字段：
  - title / type / incentive
  - active / start_time / end_time
  - blacklist
  - inviter_has_incentive / new_user_has_incentive

---

### ec_activity_coupon（活动-券关联）
- 字段：
  - activity / coupon / total / type

---

### ec_coupon（优惠券）
- 字段：
  - title / content
  - expiry
  - issue_amount / issue_remain
  - min_amount / discount / max_discount
  - state

---

### ec_coupon_issue（发放记录）
- 用于订单与优惠券关联

---

### ec_coupon_customer（用户券）
- 字段：
  - state / used_time / order_id

---

### ec_coupon_service（适用服务）
- 券适用范围（服务维度）

---

### ec_coupon_station（适用站点）
- 券适用范围（站点维度）

---

### ec_customer_tag（用户标签）
- 客户标签关系（待进一步确认使用）

---

## 五、站点 / 公司 / 字典 / 反馈

### ec_station（站点表）
- 字段：
  - 地址 / 经纬度 / 营业时间
  - park_number / park_price / park_free
  - 终端类型

---

### ec_company（公司）
- 用于订单/统计/权限

---

### ec_company_belong（公司层级）
- 用于权限与统计

---

### ec_wordbook（字典表）
- 用途：
  - 类型 / 状态 / 标准 / 功率档映射

---

### ec_information_fee_back（反馈/发票）
- 字段：
  - type / company / content / urls
  - phone / openid
  - invoice_title / 银行信息
  - total / consignee / address

---

### ec_information_fee_back_orders（反馈订单明细）
- 字段：
  - order_id / order_no
  - station_name / order_time
  - total / duration